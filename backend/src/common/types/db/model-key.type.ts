import {
  UserKey,
  SettingsKey,
  RoomKey,
  RefreshTokenKey,
  CommonKey,
} from 'common/enums';

type ModelKey = UserKey | SettingsKey | RoomKey | RefreshTokenKey | CommonKey;

export type { ModelKey };
